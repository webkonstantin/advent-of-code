import { getInput } from './get-input';
import * as assert from 'node:assert';

const input = await getInput();

const part1 = (input: string) => {
};

assert.equal(part1(``.trim()), 0);

console.log(part1(input));
